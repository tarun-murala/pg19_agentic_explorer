[metadata]
name = pg19-llm-client
version = 0.1.0

description = Thin Ollama LLM client for PG19-Agentic-Explorer

[options]
packages = find:
python_requires = >=3.10
install_requires =
    httpx

[options.packages.find]
where = .
