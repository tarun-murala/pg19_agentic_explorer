# UI Service

Next.js 14 app router front-end that provides chat interactions, agent trace visualization, and KG exploration widgets.

## Dev setup (to be fleshed out later)
1. Install dependencies: `npm install`.
2. Run the dev server: `npm run dev`.

The UI consumes the Orchestrator REST API and websockets to render chat threads and agent timelines.
